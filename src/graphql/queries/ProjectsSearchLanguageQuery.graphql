#import '../fragments/ProjectsContainer_project.graphql'

query ProjectsSearchLanguageQuery($languages: [String!]) {
  searchProjects(languages: $languages) {
    totalCount
    edges {
      node {
        ...ProjectsContainer_project
      }
    }
  }
}
